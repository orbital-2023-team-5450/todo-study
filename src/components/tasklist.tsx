import React from "react";
import { Typography } from "@mui/material";

export default function TaskList({ tasks } : { tasks : string[]}) {

    return ( <Typography variant="h2" component="h2"> yoloooooooo </Typography> );
}