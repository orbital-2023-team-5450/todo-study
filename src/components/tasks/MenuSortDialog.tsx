import React from 'react';
import { Dialog, DialogTitle, DialogContent } from "@mui/material";

export default function MenuSortDialog({menuSortOpen} : {menuSortOpen : boolean}) {
    
    return (
        <Dialog open={menuSortOpen}> 
            <DialogTitle>

            </DialogTitle>
            <DialogContent>

            </DialogContent>
        </Dialog>
    );
}