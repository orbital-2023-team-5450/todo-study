import React from "react";
import { Typography } from "@mui/material";;

export default function Timer() {

    document.title = "Timer  // TODO Study";

    return (
        <Typography>This is a timer.</Typography>
    );
}